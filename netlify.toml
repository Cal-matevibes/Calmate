[build]
  base    = "frontend"
  command = "npm install && npx react-scripts build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"
  CI = "false"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
